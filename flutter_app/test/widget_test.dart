// Smoke test — full integration tests require Firebase/Supabase initialisation
// and run via flutter_test with a real device/emulator.
// Unit tests live in test/models/, test/i18n/, and test/config/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('unit test suite placeholder', () {
    expect(true, isTrue);
  });
}
